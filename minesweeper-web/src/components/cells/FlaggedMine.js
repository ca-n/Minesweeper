import React from 'react'
//import { loadImage } from '../utils/ImageLoader';
import Button from "../Button";
const FlaggedMine = () => {
    return (
        <Button inner={'#'}
                foreground={'black'}
                background={'green'}
                onLeftClick={() => {}}
                onRightClick={() => {}} />
    )
}

export default FlaggedMine
